% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{extractKernelValues}
\alias{extractKernelValues}
\title{Extract Kernel Values of a Raster Layer (Internal function)}
\usage{
extractKernelValues(raster, xs, ys, kernelDim, kernelFun)
}
\arguments{
\item{raster}{SpatRaster. The raster from which values are extracted.}

\item{xs}{numeric vector. x coordinates of query points.}

\item{ys}{numeric vector. y coordinates of query points (same length as \code{xs}).}

\item{kernelDim}{odd positive integer. Side length of the square kernel window.}

\item{kernelFun}{function. Aggregation function applied to each kernel window
(e.g. \code{mean}, \code{max})}
}
\value{
numeric vector of length \code{length(xs)} with one aggregated value
per query point.
}
\description{
For each coordinate pair in \code{xs}/\code{ys}, extracts raster values
within a square neighborhood of size \code{kernelDim} × \code{kernelDim}
pixels and aggregates them with \code{kernelFun}.
Raster borders are toroidaly wrapped.
}
\author{
Mika Schubert
}
